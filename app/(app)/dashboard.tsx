import { PlaceholderScreen } from "@/components";

/** Tier-gated home: free shows cals vs 2,000; premium adds macros/target/streak. */
export default function Dashboard() {
  return <PlaceholderScreen title="Dashboard" subtitle="Calories, macros, streak" />;
}
