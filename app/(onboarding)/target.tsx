import { PlaceholderScreen } from "@/components";

/** S6: personalized target + projection graph (server-computed). */
export default function Target() {
  return <PlaceholderScreen title="Your target" subtitle="Step 6 of 8" />;
}
