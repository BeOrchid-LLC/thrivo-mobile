import { PlaceholderScreen } from "@/components";

/** S3: goal selection (lose / maintain / gain). */
export default function Goal() {
  return <PlaceholderScreen title="Your goal" subtitle="Step 3 of 8" />;
}
