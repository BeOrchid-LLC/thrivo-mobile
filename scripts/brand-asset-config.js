const path = require("path");

const SPLASH_CANVAS_SIZE = 1024;
const SPLASH_MARK_SIZE = 460;
const SPLASH_MARK_SOURCE = path.join("src", "assets", "thrivo-mark.svg");

module.exports = {
  SPLASH_CANVAS_SIZE,
  SPLASH_MARK_SIZE,
  SPLASH_MARK_SOURCE,
};
